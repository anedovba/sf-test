{% extends 'AppBundle::layout.html.twig' %}

{% block body %}
   <h1>Articles list</h1>
   <hr>
   {%for article in articles %}
   <h3><a href="{{ path('article_page', {id:article.id})}}"> {{article.title}}</a></h3>
   {{article.content}}
   {%endfor%}
{% endblock %}
{% block title %}
    Hello!
{% endblock %}